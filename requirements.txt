# Backend (scraper + bootstrap)
requests>=2.31
python-dotenv>=1.0
gspread>=6.0
google-auth>=2.29
googlemaps>=4.10        # Maps (requer billing); só usado se chave funcionar
geopy>=2.4              # Nominatim (OpenStreetMap) — fallback gratuito

# Frontend (Streamlit dashboard - Fase 4)
streamlit>=1.50          # width='stretch'/'content' (use_container_width removido)
pandas>=2.0
pydeck>=0.9

# IA — Resumo de editais via Google Gemini API (gratuito) (Fase 7)
google-genai>=1.0          # novo SDK Google (substitui google-generativeai)
pdfplumber>=0.10

# Exportação para Excel (.xlsx) no dashboard (modelo ConLicitação)
openpyxl>=3.1
